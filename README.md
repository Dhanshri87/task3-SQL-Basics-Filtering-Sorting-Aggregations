# task3-SQL-Basics-Filtering-Sorting-Aggregations
SQL Queries Performed
1)Data Verification

Used SELECT * to explore the dataset

Verified total number of records using COUNT(*)

2️) Filtering & Sorting

Applied WHERE clause to filter data by category and region

Used ORDER BY to identify top-selling products

3)Aggregations

Calculated total sales using SUM(sales)

Calculated average profit using AVG(profit)

Counted total orders using COUNT(*)

Grouped results using GROUP BY (category, region)

4) HAVING Clause

Filtered aggregated results (e.g., categories with sales > 100000)

5)Date & Pattern Filtering

Used BETWEEN for monthly sales reports

Used LIKE to search customer names by pattern



